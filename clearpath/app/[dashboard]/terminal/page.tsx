"use client";
import { useRef, useState } from "react";

export default function Terminal() {
  return (
    <main className="flex min-h-screen flex-col items-center justify-between p-4 text-gray-900"> 
    </main>
  );
}